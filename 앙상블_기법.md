# 퀀티랩 엔진 앙상블 기법 (Sharpe 선택 방식)

## 어떤 기법을 적용했나

**검증구간 Sharpe ratio 기반 에이전트 선택 + 롤링 재선정 + turbulence 위험회피** 앙상블.

출처 논문: **Hongyang Yang, Xiao-Yang Liu, Shan Zhong, Anwar Walid,
"Deep Reinforcement Learning for Automated Stock Trading: An Ensemble Strategy", ICAIF 2020.**
(자료조사/유사 연구사례 폴더에 있는 논문. `AI트레이딩_의사결정.docx`의 권장안 —
"A2C·PPO를 추가하고 검증구간 Sharpe 기반 앙상블 선택과 turbulence 위험회피 규칙을
도입해 위험조정수익을 높인다" — 를 그대로 이행한 것이다.)

기존에 쓰던 다수결(매 스텝 3개 모델 투표)은 논문에 근거가 없는 단순 방식으로,
성향이 다른 모델들의 행동이 서로 상쇄되어(예: 한 모델은 추세추종 매수, 다른 둘은 관망)
개별 모델보다 성과가 나빠지는 문제가 있었다. Yang et al.은 "모델을 섞지 말고,
**최근 위험조정수익이 가장 좋은 모델 하나에게 운용을 맡기고 주기적으로 교체**"하는
방식을 제안했고, 논문 실험에서 개별 PPO/A2C/DDPG·다우지수·최소분산 포트폴리오를
모두 이기는 Sharpe 1.30을 기록했다.

## 어떻게 작동하나 (구현: backend/app/engine.py)

1. **개별 학습** — 선택한 알고리즘(DQN/A2C/PPO 중 2~3개)을 각각 독립적으로 학습한다.
   이때 학습 구간의 마지막 부분(최대 63거래일, 학습 구간의 최대 20%)을 **검증 구간**으로
   분리해 학습에서 제외한다(out-of-sample 검증, 논문 Step 1·2).
2. **초기 리더 선정** — 각 모델을 검증 구간에서 단독 백테스트해 **연환산 Sharpe ratio**가
   가장 높은 모델을 초기 리더로 선택한다 (논문 Step 2, 식 (19)).
3. **테스트 운용 + 롤링 재선정** — 리더 모델이 실제 포트폴리오를 운용한다. 동시에 모든
   멤버가 각자 **그림자(shadow) 포트폴리오**를 병렬로 운용하고, `ensemble_window`
   거래일(기본 21일)마다 직전 구간 그림자 Sharpe가 가장 높은 모델로 리더를 교체한다.
   논문은 "분기마다 재학습 후 검증 Sharpe로 선택"하지만, 본 프로토타입은 1회 학습
   구조이므로 "최근 실전 성과 기반 롤링 재선정"으로 적응했다 (논문 Step 3의 취지:
   상승장/하락장/횡보장마다 잘하는 에이전트가 다르므로 시장 국면에 따라 교체).
4. **Turbulence 위험회피** — 논문 식 (3)의 turbulence 지수(자산 수익률의 마할라노비스
   거리)가 임계값을 넘으면 **전량 매도 후 매수 중단**, 지수가 내려오면 재개한다
   (논문 식 (10), 2008·2020 폭락장 방어 규칙). 원 논문은 30종목 공분산 기반이지만
   본 프로젝트는 단일 종목이므로 1차원으로 축약했다:
   `turbulence_t = ((r_t − μ)/σ)²` (최근 252일 수익률 분포 대비 당일 수익률의 표준화
   제곱). 임계값은 학습 구간 turbulence의 95분위수로, 과거 데이터만 사용해 룩어헤드가
   없다.

## 논문 대비 적응(변경)한 점

| 항목 | 논문 | 본 구현 | 이유 |
|---|---|---|---|
| 멤버 | PPO·A2C·DDPG | DQN·A2C·PPO | 의사결정 문서: DQN 베이스라인 유지, DDPG는 연속행동용이라 이산행동(매수/관망/매도) 구조에 불필요 |
| 재선정 | 분기별 재학습+검증 | 1회 학습 + 21일 롤링 그림자 Sharpe | 프로토타입 실행시간 제약. 재학습 없이 같은 취지(국면별 최적 에이전트 교체) 구현 |
| turbulence | 30종목 공분산 마할라노비스 | 단일종목 표준화 제곱 z² | 단일 종목 백테스트라 공분산 행렬이 정의되지 않음 |
| 대상 | 다우 30종목 포트폴리오 | 국내 단일 종목 | 프로젝트 범위(README 의사결정①) |

## 사용법

프론트에서 퀀티랩 엔진 선택 → 알고리즘 2~3개 선택 → 앙상블 방식에서
**"Sharpe 선택 (Yang et al. ICAIF 2020)"** (기본값) 선택. 고급 설정에서 리더 재선정
주기(5~126일) 조절 가능. 기존 다수결은 "다수결 투표" 버튼으로 비교 실험용으로 남겨두었다.

API 파라미터: `algorithms`(1~3개), `ensemble_method`("sharpe"|"vote"),
`ensemble_window`(기본 21), `use_turbulence`(기본 true).

결과 JSON의 `ensemble` 필드에 멤버별 검증 Sharpe, 초기 리더, turbulence 임계값,
리더 교체 이력(`leader_timeline`)이 포함된다.

## 검증 결과 (삼성전자, 학습 2022-01~2024-06 / 테스트 2024-07~2025-06, seed=11, 8에폭)

| 방식 | 최종 PV (1,000만 시작) | 수익률 |
|---|---|---|
| 단순보유 (Buy&Hold) | 686만 | −31.3% |
| 다수결 앙상블 (기존) | 696만 | −30.4% |
| **Sharpe 선택 앙상블 (신규)** | **830만** | **−17.0%** |

하락장 테스트 구간에서 turbulence 매도 규칙과 리더 교체(DQN↔PPO↔A2C, 8회)가
낙폭을 절반 가까이 줄였다. (에폭 수를 늘리면 개별 모델 품질이 올라가 차이는 달라질 수 있음)
